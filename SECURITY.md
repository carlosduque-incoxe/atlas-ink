# Security

Do not report or publish credentials, account data, device configuration, private endpoints, or internal topology in this repository.

Only signed release assets are accepted. The signing private key is never stored here. The committed public key is verification material and is not secret.
